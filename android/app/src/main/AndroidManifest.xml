<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
            Type `connectedDevice` : c'est l'unique catégorie legale pour du BLE
            depuis Android 14. Elle est declaree des maintenant, alors que
            l'etage 1 du PoC ne touche pas au Bluetooth, parce que c'est
            justement cette declaration qu'on teste — un service d'un autre type
            se comporterait autrement face a One UI.
        -->
        <service
            android:name=".SurvieService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
        Le type `connectedDevice` exige que l'application detienne au moins une
        permission de la liste prevue par Android : `BLUETOOTH_CONNECT` la
        satisfait. A l'etage 1, elle n'etait pas encore demandee a l'execution
        et `CHANGE_NETWORK_STATE` tenait ce role sans rien demander a
        l'utilisateur ; l'etage 2 demande les deux permissions Bluetooth pour de
        bon, la bequille est donc retiree.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!--
        `neverForLocation` : nous cherchons une ceinture cardiaque, jamais une
        position. Sans ce drapeau, Android exigerait la permission de
        localisation pour une recherche Bluetooth — une demande incomprehensible
        pour l'utilisateur, et une donnee dont nous n'avons aucun usage.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Avant Android 12, aucune recherche Bluetooth n'etait possible sans elle. -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
</manifest>
